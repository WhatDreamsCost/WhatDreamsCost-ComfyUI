# Log

Implemented and verified legacy workflow import compatibility:

- Import now accepts current story script JSON and legacy full workflow JSON.
- Legacy workflow nodes are converted into story script node entries using widget names.
- Old Long Auto timelines are normalized with current `longAuto` meta defaults and output prefixes.
- README documents that old full workflow JSON, such as old `long-auto.json`, can be imported.

Validation passed with JS syntax check, diff check, and static LTXDirector widget mapping check.
