# Request

User asked to:

- Delete `pro-workflows/camera.json`.
- Re-check LTX 2.3 video reference / IC-LoRA behavior for camera/motion reference:
  1. Whether IC-LoRA supports segmented or only single-span guides.
  2. Whether single-span guides require explicit timeline alignment.
  3. Whether multi-span guide handling can be implemented like audio slicing.
  4. How video reference relates to keyframes.
  5. Whether camera reference and motion/action reference can be provided together.
  6. Whether the two reference streams can be interleaved across time.
- Record the conclusion in `.agent/res` in Chinese and English.
