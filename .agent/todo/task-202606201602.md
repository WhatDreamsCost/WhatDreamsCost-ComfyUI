# Task

Scene: workflow-cleanup

Target: camera-delete-ltx23-video-reference

## Goals

- Remove the obsolete single-control `camera.json` workflow.
- Update README references so only the current workflow set is documented.
- Inspect official/local LTX 2.3 IC-LoRA node implementation and current project implementation.
- Record a bilingual conclusion in `.agent/res`.

## Verification

- Confirm `camera.json` is deleted from `pro-workflows`.
- Parse retained workflow JSON files.
- Check README for stale `camera.json` active workflow references.
- Run `git diff --check`.
