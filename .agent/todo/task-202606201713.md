# Task

Scene: story-script-import

Target: legacy-workflow-import

## Goals

- Allow Meta Info Import to accept both `*-ss.json` story scripts and old full workflow JSON files.
- Extract content widgets from legacy workflow nodes without replacing current graph structure.
- Normalize old Long Auto timeline metadata to the current `ltx-director-pro` defaults.
- Document the compatibility behavior.

## Verification

- `node --check js/story_script.js`
- `git diff --check`
- Static mapping check for current LTXDirector widget order.
